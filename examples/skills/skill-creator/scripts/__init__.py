"""skill-creator helper scripts.

Run from the skill-creator directory as a module, e.g.:

    python -m scripts.aggregate_benchmark <iteration-dir> --skill-name <name>
    python -m scripts.package_skill <skill-folder>
    python -m scripts.run_loop --eval-set q.json --skill-path <skill> --model <id>
"""
